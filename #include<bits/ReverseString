#include<bits/stdc++.h>
using namespace std;

string reverseWord(string str){
    if(str.length()==0){
        return str;
    }
    string revofstr=str.substr(1);
    reverseWord(revofstr);
    cout<<str[0];
}

int main(){

    string str="cool";
    reverseWord(str);
  
    return 0;
}
